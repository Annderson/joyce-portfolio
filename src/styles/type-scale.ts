import styled from 'styled-components'
import dimensions from './dimensions'

const SansFont12 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size12};
  margin-bottom: ${dimensions.px.size0};
`

const SansFont14 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size14};
  margin-bottom: ${dimensions.px.size0};
`

const SansFont18 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size18};
  margin-bottom: ${dimensions.px.size0};
`

const SansFont20 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size20};
  margin-bottom: ${dimensions.px.size0};
`

const SansFont25 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size25};
  margin-bottom: ${dimensions.px.size0};
`

const SansFont30 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size30};
  margin-bottom: ${dimensions.px.size0};
`

const SansFont35 = styled.label`
  font-family: sans-serif;
  font-size: ${dimensions.px.size35};
  margin-bottom: ${dimensions.px.size0};
`

export default {
  SansFont12,
  SansFont14,
  SansFont18,
  SansFont20,
  SansFont25,
  SansFont30,
  SansFont35
}
